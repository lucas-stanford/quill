import type { ApproveApi } from "./useApprove";

/** FROZEN PROP CONTRACT — rendered in the title bar. */
export interface ApproveButtonProps {
  approve: ApproveApi;
}

// STUB — replaced by the approve workstream.
export function ApproveButton(_props: ApproveButtonProps) {
  return null;
}
